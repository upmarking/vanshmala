## 2024-05-16 - [React.memo in Recursive Tree Components]
**Learning:** Highly recursive components like `TreeNode` in family tree visualizations suffer from catastrophic cascading re-renders if parent state changes (e.g., opening a dialog).
**Action:** Always wrap recursive visual components in `React.memo()` and ensure handlers passed down to them are wrapped in `useCallback` to prevent deep reconciliation cycles.2024-04-24 - [Optimizing GiftCard string generation], Learning: Replacing string concatenation inside a loop with an unrolled bitwise access using `Uint8Array` populated by `window.crypto.getRandomValues()` yields up to ~17-20% performance improvement while making it cryptographically secure. Dummy benchmarking files generated during optimization steps should be deleted to keep the project clean before review., Action: Centralized `generateCode` utility in `src/utils/crypto.ts` and successfully swapped inline `Math.random` implementations across components. Always clean up temporary files in root directory.
